export async function callOpenAIChat(messages: any[], model?: string): Promise<any> {
  // TODO: Implement real OpenAI chat with server-side API key
  return { ok: true, message: 'Mock response from OpenAI' };
}
