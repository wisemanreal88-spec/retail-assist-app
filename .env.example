# Retail Assist App - Environment Variables

# ============================================
# PUBLIC VARIABLES (visible in browser)
# ============================================

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Mock mode (set to "true" for local development with mock data)
NEXT_PUBLIC_USE_MOCK_SUPABASE=true

# Test mode (set to "true" to enable test endpoints)
NEXT_PUBLIC_TEST_MODE=false

# ============================================
# PRIVATE VARIABLES (server-side only)
# ============================================

# OpenAI
OPENAI_API_KEY=sk-your-api-key-here

# Meta/Facebook Integration
META_PAGE_ACCESS_TOKEN=your-meta-page-token-here
META_VERIFY_TOKEN=your-meta-verify-token-here

# Supabase (server-side)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# DEPLOYMENT NOTES
# ============================================
# 
# Local Development:
# - Set NEXT_PUBLIC_USE_MOCK_SUPABASE=true to use mock data
# - Copy this file to .env.local and fill in values
#
# Netlify Production:
# - Set variables in Netlify UI > Site settings > Build & deploy > Environment
# - NEXT_PUBLIC_USE_MOCK_SUPABASE should be "false" when using real Supabase
# - Keep private keys (OPENAI_API_KEY, etc.) secure in Netlify only
#
